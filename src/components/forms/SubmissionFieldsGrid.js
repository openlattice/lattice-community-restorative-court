// @flow
import styled from 'styled-components';

const SubmissionFieldsGrid = styled.div`
  display: grid;
  grid-gap: 10px;
  grid-template-columns: repeat(2, 200px);
`;

export default SubmissionFieldsGrid;
